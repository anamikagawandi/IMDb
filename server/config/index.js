module.exports = {
    mongodb:{
        username: encodeURIComponent(''),
        password: encodeURIComponent(''),
        db: "imdb",
        defaultLimit: 10,
        sortOrder: {creation_date:-1}
    }
}
